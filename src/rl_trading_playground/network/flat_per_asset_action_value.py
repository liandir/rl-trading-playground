"""Flat per asset action value utilities for neural network architectures and reusable model components."""
from rl_trading_playground.network.core.per_asset import BaseFlatPerAssetActionValueNetwork


class FlatPerAssetActionValueNetwork(BaseFlatPerAssetActionValueNetwork):
    """FlatPerAssetActionValueNetwork network module for neural network architectures and reusable model components."""
    name = "flat_per_asset_action_value"
